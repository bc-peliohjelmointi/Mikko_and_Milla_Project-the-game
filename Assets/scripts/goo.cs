using UnityEngine;

public class goo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void Foo() { Debug.Log("jfdsjd"); }

    // Update is called once per frame
    void Update()
    {
        
    }
}
